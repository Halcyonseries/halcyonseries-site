# 1 – show what changed (optional)
git status

# 2 – stage all modified / new files
git add .

# 3 – commit with a brief message
git commit -m "Add lightbox arrows and YouTube video grid"

# 4 – send the commit to GitHub
git push